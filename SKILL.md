---
name: pdftoexcel
description: 把中文价格表/报价单/画册类 PDF 原样转成 Excel——保持表格线、合并单元格、版式间距与产品照片，并支持折价、清空单价、A4 一页一张打印。用户说"把某个 PDF 转成 Excel/照原格式做一份表格/这个价格表打个折/单价留空/要能直接打印"时使用本技能。
---

# PDF → Excel 原版式转换

难点不在"提取文字"，而在**表格几何**和**版式一致性**——用户拿它是要对着原件逐格核对、
甚至直接当报价单打印的。按下面 6 个阶段做，每阶段产物落盘后再进下一阶段（用户可能中途停）。

工作目录**不用手设**：默认落在 `~/.qwen/tmp/<PDF 名>/`（Windows 上是 `%USERPROFILE%\.qwen\tmp\...`），
里面自动建 `cells\`（几何 g*.json、模型 m*.json、叠加图 v*.png）、`ocr\`、`imgs\`。
换机器、换用户名都不用改任何东西；要放别处就加 `--work <目录>`，或设环境变量 `PDFTOEXCEL_WORK`。

装依赖（只做一次）：Windows 跑 `install.bat`，macOS/Linux 跑 `sh install.sh`。
只有"无文字层"的 PDF 才需要 `rapidocr-onnxruntime`，缺它其余流程照跑；需要时单独执行 `python -m pip install --user -r requirements-ocr.txt`。

## 一条流程（各阶段的确切命令）

```
python scripts/pdftoexcel.py probe  --pdf "输入.pdf"              # 阶段 1：几何 + 路线判决
python scripts/pdftoexcel.py ocr    --pdf "输入.pdf" --pages 2,3  # 仅 outline-ocr 页需要
python scripts/pdftoexcel.py model  --pdf "输入.pdf"              # 阶段 2/3：出 cells/m*.json
python scripts/pdftoexcel.py xlsx   --pdf "输入.pdf"              # 阶段 4：默认写 ~/Desktop/<同名>.xlsx
python scripts/pdftoexcel.py check  --pdf "输入.pdf"              # 阶段 5：结构 + 逐格显示值
```
`--pages`（0 基，逗号分隔）、`--work`（工作目录）、`--out`（输出文件）、`--dpi`（照片清晰度，
默认 300）、`--font`（默认 宋体）、`--auto-slash`/`--slash`/`--no-slash` 都可加；`xlsx` 会用 `--dpi` 裁照片，25 页画册 300dpi 约 14MB。

## 阶段 0：一次问清 3 件事

1. **原样 / 折价 / 单价留空**——只有"单价"列参与折算或清空；规格列（`25×20`、`40抢修节`）和页码原样。
   折价默认保留 2 位小数四舍五入、不保留原价；留空模板默认把单价里的占位横杠（`-`/`—`/`----`）一并清掉。
2. **输出文件名**（默认与 PDF 同名放桌面）。
3. **照片**：默认按原位置嵌入单元格。多页画册要先问 dpi——**300dpi × 25 页能到 15MB+**，
   200dpi 通常足够打印。

## 阶段 1：探路，先定路线再动手

```
python scripts/pdftoexcel.py probe --pdf "输入.pdf"          # 加 --pages 0,1,2 只探部分
```

每页会给出一行 `route:` 判决，照它走：

| route | 含义 | 下一步 |
|---|---|---|
| `text-layer` | 有文字层 | 文字/字号/颜色**直接取 chars，不要 OCR，不要做字号标定** |
| `outline-ocr` | 无文字层、curves 上万 | 文字是矢量描边 → 双路径识别（阶段 2B） |
| `no-grid` | 没检出格子 | 封面/照片墙：用图片与文字边界合成网格（cover）；否则说明线没认出来 |

**格子数对了但"重叠数 = 格子数 − 1"是特征信号**：页面外框线被当成表格线画了，最外层那一大片
连通域被误判成一个格子。修法是在提取线段时排除贴页面四边的线（本脚本已内置）。

另外三条：

- 表格线有**三种画法**，只扫一种必翻车：逐格细矩形（CorelDRAW 常见，w>5pt、h≈0.25pt）、
  真正的 `lines` 描边、多点描边路径（一整张表画成一条 zigzag path）。
  → **一律扫 `page.edges`**（描边线 + 每个 rect/curve 四条边的超集）。
- **`find_tables()` 只能当交叉验证**，不能用来取格子：实测同一文件里它给 10 个错表、
  或把两页各合成 1 个巨表。
- **必须人工看一眼 `cells\v*.png`**：蓝框要压在真实表格线上，合并格/照片格不能被拆成多格。
  这步不确认，后面全是白做。

## 阶段 2：取内容

### 2A 有文字层（首选，零识别误差）

按单元格矩形把字符分配进去，行内按 x 排序、间隙 >1.6pt 补空格。
**注意：页标题常常落在表格第一个合并格里**（标题带画在表框内），不在页边自由文字里。

字符级去重：描边+填充两遍绘制会让标题变成 `AABBCC`（同一字被描边和填充各画一遍）。判别是"同一个字、x 原点相差 <0.6pt"，
必须丢掉重复的那份——但**别按"相邻字符相同"去重**，`PPR` 的两个 P 是真内容。

### 2B 无文字层：两条识别路径并用

- **A 整页 det+rec**（300dpi）：能读中文名称标签和烤进照片里的字；**窄列 1–2 位数字会漏**（实测 7–27 个/页）。
- **B 矢量描边聚类成词框 + 纯识别**（`use_text_det=False`）：覆盖完整、几何精确；漏中文标签，`/` 识成空。

取并集时两个坑：

1. 冲突时**优先 B**，但不能"整体偏 B"——那会吞掉只有 A 读到的照片文字。
   只在**一个 B 框被 ≥2 个同线 A 框合计覆盖 ≥80%** 时丢那几个 A 框（否则会出现
   `S200S200 C款）C款` 这种重复拼接）。"覆盖"要按区间**并集**算，不能拿单个框比。
2. 改完必须 **diff 新旧模型**，确认只有目标格变化、填充数不变。

RapidOCR 静默失效的坑：`use_text_det/use_angle_cls` 是**实例属性**不是调用参数；
`text_score` 默认 0.5 会**悄悄丢掉**低分结果 → 设 `0.0`；返回的 score 是**字符串**。
**废弃路线**：逐格裁剪再跑 det（小图上检不出，实测 58/389）。

## 阶段 3：字号——只在"无文字层"时才需要标定

有文字层时字号是现成的，**跳过本阶段**。没有文字层时，别用"识别框高 ÷0.72"（实测把 12pt 算成 15–16pt）。
拿有文字层的那一页标定 ink/em，再反解：

```
python scripts/pdftoexcel.py fontsize --pdf 输入.pdf --page N --rect x0,y0,x1,y1
```

**TODO(verify)** 上次在同一套中文字体上测到 **CJK ink/em = 0.917、数字/西文 cap = 0.681**，
代码里就是这两个值。**换机器或遇到别的字体（仿宋/楷体/思源）请先复测**：拿一页有文字层的 PDF
跑上面的 `fontsize`，如果测出的字号与 PDF 声明值差超过 1pt，就去 `scripts/stages.py`
改 `CJK_INK_RATIO` / `CAP_INK_RATIO`。

## 阶段 4：出表

一页一个 sheet。核心几何规则：

- **扩展网格** `XG = [0] + xs + [W]`（行同理），页面空白边也变成行列，页眉页脚文字和表外照片才有格子落。
  **原网格索引 i → Excel 列号 = i + 2**（插了 0 列；写成 i+1 整张表左移一列）。
- **列宽按边界吸附整像素**：`px = round(pt*4/3)` 作用在**边界**上再取差分（不是按带宽取整，
  那样会累计漂移，实测累计能到 2.4pt；吸附后 ≤0.37pt）。
  **边界最小间隔 6px**——低于 6px 的列在 width 单位里表达不出来，被钳成别的值会让整页右移（实测 4.7pt）。
  行高直接用 pt，无累计误差。
- 合并区 = 单元格 `[i0,j0,i1,j1]`；**合并区内部每个格都要设 thin 边框**，否则合并后表格线会缺。
  关闭 `showGridLines`。
- **垂直对齐**：含照片的合并格用 `bottom`（原件普遍是"照片在上、名称在下"），其余 `center`。
- **数字写成数值 + `number_format`**（按原文小数位，`6.70` 必须仍显示 6.70），这样能直接筛选/套公式。
  带前导零的退回文本。
- 颜色按模型设 red/blue。
- **照片**：从渲染图按 bbox 裁 PNG，用 `OneCellAnchor`（单元格 + EMU 偏移，1pt = 12700 EMU）。
  别用 `add_image(ws, "B12")` 字符串锚点（尺寸会变成图片原始像素）。
  **每页渲染图用完就释放**——多页 300dpi 全cache会吃到几百 MB。
- **sheet 命名**：从 **free + cells 全部文字**里取「字号 ≥14、长度 ≥8、含中文、不是纯数字单位串」中最大的那条；
  封面/照片墙页（格子 <20）把长度门槛降到 4；都没有就用 `第N页`。
  清洗：去 `■`、合并 CJK 之间空格、`/`→`·`；重名加 `第N页`；**截断到 31 字符**。
- **"无货"符号**：只在没有文字层的文件上按墨迹形状推断（空格子里 ink 宽 2.5–9pt、高 7–14pt → `/`）。
  **有文字层时关掉这个推断**——原件用的是 `–` 破折号，文字层读得到。
  别照抄上一份文件的坐标：同类文件里一份是 p3 有 6 格、p4 一格都没有。
- 打印：A4(`paperSize=9`)、`portrait`、`fitToWidth=fitToHeight=1` +
  `sheet_properties.pageSetUpPr = PageSetupProperties(fitToPage=True)`、页边距 0.2、`print_area` 到实际使用区。

## 阶段 5：核对（三道都要跑）

```
python scripts/pdftoexcel.py check --pdf 输入.pdf --xlsx 输出.xlsx     # --pdf 可省，改 --grid <工作目录>\cells
```

1. **结构**：合并区两两求交必须为 0（重叠会让 Excel 弹"修复"，而 openpyxl 写侧读侧都不报错）、
   列边界与 PDF 偏差（应 ≤0.4pt）、打印设置、图片数。
2. **逐格内容**：把每个格子的值与模型比——**必须按 `number_format` 比"显示值"**，
   不能比 `str(value)`（`72` + `0.00` 显示 `72.00`，比原始值会误报上百处）。
   无文字层页再加 A/B 两路对照 + 第三路（数字格 ink 单独 rec）复核；
   第三路告警先当误报处理（单字符裁剪常识成空或带相邻列残字），**裁出带上下文的图亲眼看**。
3. **列角色合理性**：单价列必须像金额、数量列必须像整数——不合规通常是识别错，或者那只是个重复表头。

**Excel 硬限制**（写之前记住）：sheet 名 ≤31 字符且禁 `\ / * ? : [ ]`；列宽 <6px 表达不了；
行高上限 409.5pt。

**这台机器装了 Excel / LibreOffice 吗？** 先 `where soffice` / `where excel` 问一句：
没有的话，上面三道都是反推校验——交付时如实说明"没做视觉比对"，请用户打开看**打印预览**
（每页应正好一张 A4）。**有就要多做一步**：把 xlsx 转成 PDF 与原件逐页对照，
那是唯一能真正发现位置错位的检查，别只跑反推就交。

## 阶段 6：落盘

- 写 `HANDOFF.md`：已验证的结论 / 剩余步骤 + 确切命令 / 风险与待确认项。模型 JSON 是下一天的起点，**不要重做几何**。
- 用户说"我要下班了/明天继续"时**立刻停手写交接**，别赶交付物。
- 记忆里只留指向 HANDOFF 的指针和跨项目事实；**只对本文件成立的数字（格子数、`/` 位置、字号）
  属于 HANDOFF，不属于记忆**，也不要当成下次的结论套用。

## 参考实现

`scripts/pipeline/` 是两份真实文件（一份 5 页价格表和一份 25 页产品画册）跑通版的原样存档，
**只读不用**——里面还留着原先那台机器的路径写法（已改为可移植常量），说明见 `scripts/pipeline/README.md`。
可复用的实现都在 `pdftoexcel.py` + `stages.py`，路径全部由 `_paths.py` 推导，没有一处需要为新机器修改。
细节与踩坑清单见 [reference.md](reference.md)。


